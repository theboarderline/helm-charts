{{- if .Values.gatekeeper.enabled }}

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gatekeeper
  namespace: {{ .Values.namespace }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.root_project }}
  destination:
    server: {{ .Values.destination.server }}
    namespace: {{ .Values.gatekeeper.destination.namespace }}
  source:
    {{- if .Values.gatekeeper.source.chart }}
    chart: {{ .Values.gatekeeper.source.chart }}
    {{- else }}
    path: {{ .Values.gatekeeper.source.path }}
    {{- end }}
    repoURL: {{ .Values.gatekeeper.source.repoURL }}
    targetRevision: {{ include "gatekeeper.targetRevision" . }}
  helm:
    valueFiles:
      - "values/{{- .Values.lifecycle }}.yaml"
  syncPolicy:
    automated:
      prune: {{ .Values.gatekeeper.syncPolicy.automated.prune }}
      selfHeal: {{ .Values.gatekeeper.syncPolicy.automated.selfHeal }}
    syncOptions:
      - CreateNamespace=true
      - PruneLast=false

{{- end }}
