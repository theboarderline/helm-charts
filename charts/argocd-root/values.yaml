project: default

lifecycle: dev

destination:
  server: https://kubernetes.default.svc
  namespace: argocd

argocd:
  source:
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

self:
  source:
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

root:
  source:
    targetRevision: HEAD
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
