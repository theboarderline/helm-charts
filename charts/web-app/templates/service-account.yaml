{{- if .Values.enabled }}

apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
    iam.gke.io/gcp-service-account: gke-worker@{{- include "walker-chart.gke_project" . -}}.iam.gserviceaccount.com
  name: {{ .Values.app_code }}-sa

{{- end }}
