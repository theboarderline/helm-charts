{{- if and (.Values.enabled) (.Values.importer.enabled) }}

apiVersion: v1
kind: Service
metadata:
  name: importer-svc
  namespace: {{ .Release.Namespace }}
  labels:
    namespace: {{ .Release.Namespace }}
    tier: backend
    backstage.io/kubernetes-id: {{ .Values.app_code }}
  {{- if not .Values.local }}
  annotations:
    cloud.google.com/backend-config: '{"default": "importer-backendconfig"}'
  {{- end }}
spec:
  type: ClusterIP
  selector:
    app: importer-dep
    
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: {{ .Values.importer.containerPort }}

{{- end }}
