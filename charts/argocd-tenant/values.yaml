project: default

destination:
  server: https://kubernetes.default.svc

source:
  path: web-app
  chart: ""

syncPolicy:
  automated:
    prune: true
    selfHeal: true

syncOptions:
  CreateNamespace: true
  PruneLast: false
